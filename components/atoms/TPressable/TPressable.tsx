import { TouchableOpacity, TouchableOpacityProps } from "react-native";

const TPressable = (props: TouchableOpacityProps) => {
  return <TouchableOpacity {...props} />;
};

export default TPressable;
